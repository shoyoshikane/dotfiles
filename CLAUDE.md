@AGENTS.md

## Claude Code specific notes

- When a task needs Claude-specific behavior, keep it here instead of duplicating the shared rules
- `.claude/rules/*` still contains path-scoped rules for this repo
